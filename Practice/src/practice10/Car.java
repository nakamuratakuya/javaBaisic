package practice10;

public class Car {
	int serialNo;
	String color;
	int gasoline;
	public static int run() {

		// ★ 0～15のランダムな値を取得して、変数numberに代入してください
				int limitNumber = 15;

				// ランダムで数字を生成するプログラムです。
				// 下記の命令を実行すると変数randomに、0以上、変数limitNumber未満の数字がランダムで代入されます
				int random = new java.util.Random().nextInt(limitNumber + 1);
			    int number = random;
			     int i = gasoline - number;
			   return
	}
}
